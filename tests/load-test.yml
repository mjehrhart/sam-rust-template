config:
  target: "{{ $processEnvironment.API_URL }}"
  processor: "generator.js"
  phases:
    - duration: 20
      arrivalRate: 10

scenarios:
  - name: "Generate Rapid Fire Test"
    weight: 8
    flow:
      - function: "fnName" 
      - get:
          url: "/" 
  - name: "Testing"
    weight: 2
    flow:
      - get:
          url: "/"